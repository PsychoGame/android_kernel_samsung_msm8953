--- original/sound/usb/card.c	2020-05-07 08:31:28.803760097 +0200
+++ changed/sound/usb/card.c	2019-06-03 13:39:55.000000000 +0200
@@ -678,7 +678,6 @@ static int usb_audio_probe(struct usb_in
 	chip = snd_usb_audio_probe(interface_to_usbdev(intf), intf, id);
 	if (chip) {
 		usb_set_intfdata(intf, chip);
-		usb_enable_autosuspend(chip->dev);
 		return 0;
 	} else
 		return -EIO;
