--- original/include/linux/usb/composite.h	2020-05-07 08:31:27.082775580 +0200
+++ changed/include/linux/usb/composite.h	2019-06-03 13:39:53.000000000 +0200
@@ -209,6 +209,11 @@ struct usb_function {
 	struct usb_os_desc_table	*os_desc_table;
 	unsigned			os_desc_n;
 
+#ifdef CONFIG_USB_ANDROID_SAMSUNG_COMPOSITE
+	int	(*set_intf_num)(struct usb_function *f,
+			int intf_num, int index_num);
+	int	(*set_config_desc)(int conf_num);
+#endif
 	/* REVISIT:  bind() functions can be marked __init, which
 	 * makes trouble for section mismatch analysis.  See if
 	 * we can't restructure things to avoid mismatching.
