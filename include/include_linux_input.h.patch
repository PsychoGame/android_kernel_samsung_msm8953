--- original/include/linux/input.h	2020-05-07 08:06:21.186194046 +0200
+++ changed/include/linux/input.h	2019-06-03 13:39:53.000000000 +0200
@@ -13,7 +13,14 @@
 #include <uapi/linux/input.h>
 /* Implementation details, userspace should not care about these */
 #define ABS_MT_FIRST		ABS_MT_TOUCH_MAJOR
-#define ABS_MT_LAST		ABS_MT_TOOL_Y
+#define ABS_MT_LAST		ABS_MT_GRIP
+
+/*
+ * Switch events
+ */
+
+#define SW_FLIP                 0x15  /* set = flip cover */
+#define SW_CERTIFYHALL          0x1b    /* set = flip cover attach */
 
 /*
  * In-kernel definitions.
@@ -176,6 +183,9 @@ struct input_dev {
 
 	unsigned int users;
 	bool going_away;
+	unsigned int users_private;
+	bool disabled;
+	bool lowpower_mode;
 
 	struct device dev;
 
