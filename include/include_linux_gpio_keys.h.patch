--- original/include/linux/gpio_keys.h	2020-05-07 08:06:21.140194498 +0200
+++ changed/include/linux/gpio_keys.h	2019-06-03 13:39:53.000000000 +0200
@@ -1,7 +1,7 @@
 #ifndef _GPIO_KEYS_H
 #define _GPIO_KEYS_H
 
-#define GPIO_KEYS_DEV_NAME "gpio-keys"
+#define GPIO_KEYS_DEV_NAME "gpio_keys"
 
 struct device;
 
@@ -27,10 +27,12 @@ struct gpio_keys_button {
 	const char *desc;
 	unsigned int type;
 	int wakeup;
+	int wakeup_default;
 	int debounce_interval;
 	bool can_disable;
 	int value;
 	unsigned int irq;
+	struct gpio_desc *gpiod;
 };
 
 /**
@@ -53,6 +55,7 @@ struct gpio_keys_platform_data {
 	void (*disable)(struct device *dev);
 	const char *name;		/* input device name */
 	bool use_syscore;
+	int wakeup;
 };
 
 #endif
