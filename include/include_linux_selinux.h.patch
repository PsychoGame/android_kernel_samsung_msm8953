--- original/include/linux/selinux.h	2020-05-07 08:06:21.507190892 +0200
+++ changed/include/linux/selinux.h	2019-06-03 13:39:53.000000000 +0200
@@ -24,12 +24,20 @@ struct kern_ipc_perm;
  * selinux_is_enabled - is SELinux enabled?
  */
 bool selinux_is_enabled(void);
+/**
+ * selinux_is_enforcing - is SELinux enforcing?
+ */
+bool selinux_is_enforcing(void);
 #else
 
 static inline bool selinux_is_enabled(void)
 {
 	return false;
 }
+static inline bool selinux_is_enforcing(void)
+{
+	return false;
+}
 #endif	/* CONFIG_SECURITY_SELINUX */
 
 #endif /* _LINUX_SELINUX_H */
