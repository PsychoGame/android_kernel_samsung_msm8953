--- original/drivers/usb/host/xhci-plat.c	2020-05-07 08:06:18.875216754 +0200
+++ changed/drivers/usb/host/xhci-plat.c	2019-06-03 13:39:52.000000000 +0200
@@ -218,6 +218,9 @@ static int xhci_plat_probe(struct platfo
 	if ((node && of_property_read_bool(node, "usb3-lpm-capable")) ||
 			(pdata && pdata->usb3_lpm_capable))
 		xhci->quirks |= XHCI_LPM_SUPPORT;
+#ifdef CONFIG_USB_HOST_L1_SUPPORT
+	xhci->quirks |= XHCI_LPM_L1_SUPPORT;
+#endif
 
 	hcd_to_bus(xhci->shared_hcd)->skip_resume = true;
 	/*
