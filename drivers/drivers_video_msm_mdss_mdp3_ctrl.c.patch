--- original/drivers/video/msm/mdss/mdp3_ctrl.c	2020-05-07 11:40:16.618312605 +0200
+++ changed/drivers/video/msm/mdss/mdp3_ctrl.c	2019-06-03 13:39:53.000000000 +0200
@@ -2661,10 +2661,8 @@ static int mdp3_ctrl_lut_config(struct m
 
 	dma = mdp3_session->dma;
 
-	if ((cfg->cmap.start > MDP_LUT_SIZE) ||
-		(cfg->cmap.len > MDP_LUT_SIZE) ||
-		(cfg->cmap.start + cfg->cmap.len > MDP_LUT_SIZE)) {
-		pr_err("Invalid arguments.\n");
+	if (cfg->cmap.start + cfg->cmap.len > MDP_LUT_SIZE) {
+		pr_err("Invalid arguments\n");
 		return  -EINVAL;
 	}
 
