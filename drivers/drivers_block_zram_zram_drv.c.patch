--- original/drivers/block/zram/zram_drv.c	2020-05-07 08:31:21.316827453 +0200
+++ changed/drivers/block/zram/zram_drv.c	2019-06-03 13:39:49.000000000 +0200
@@ -431,7 +431,8 @@ static struct zram_meta *zram_meta_alloc
 	}
 
 	snprintf(pool_name, sizeof(pool_name), "zram%d", device_id);
-	meta->mem_pool = zs_create_pool(pool_name, GFP_NOIO | __GFP_HIGHMEM);
+	meta->mem_pool = zs_create_pool(pool_name, GFP_NOIO | __GFP_HIGHMEM,
+					NULL);
 	if (!meta->mem_pool) {
 		pr_err("Error creating memory pool\n");
 		goto out_error;
